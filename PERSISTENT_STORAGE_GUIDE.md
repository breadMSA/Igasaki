# 永久存儲功能說明

## 📋 概述

現在所有用戶數據都會**永久保存在服務器端**，即使清除瀏覽器緩存也不會丟失！

## 🎯 保存的數據包括

1. **用戶偏好設定**
   - 用戶名稱
   - 主題（淺色/深色/自動）
   - 語言設定
   - 背景圖片和透明度
   - 用戶頭像和機器人頭像
   - TTS 模式和音量
   - Live2D 模型設定
   - AI 人格設定

2. **對話記錄**
   - 所有歷史對話（最多保留 10000 條）
   - 每條消息的時間戳
   - 消息元數據（模型、處理時間等）

3. **統計數據**
   - 總消息數
   - 對話次數
   - 最後活躍時間

## 🔧 工作原理

### 雙重存儲機制

1. **本地存儲（IndexedDB）**
   - 快速訪問
   - 離線可用
   - 作為緩存

2. **服務器存儲（文件系統）**
   - 永久保存
   - 跨設備同步
   - 數據不會丟失

### 數據流程

```
保存數據時：
用戶操作 → 本地 IndexedDB → 服務器文件 ✓
         ↓
       界面立即更新

加載數據時：
應用啟動 → 服務器文件 → 本地 IndexedDB → 顯示數據
                    ↓
                 如果服務器不可用，使用本地數據
```

## 📁 數據存儲位置

服務器端數據保存在：
```
server/data/memory/default/
  ├── preferences.json    # 用戶偏好設定
  ├── messages.json       # 對話記錄
  ├── stats.json          # 統計數據
  └── default_memory.json # 向量記憶
```

## 🚀 新功能

### 自動同步
- 每次修改設定都會自動保存到服務器
- 每條新消息都會同步到服務器
- 初始化時自動從服務器恢復數據

### 數據遷移
- 首次啟動時，如果本地有舊數據，會自動同步到服務器
- 之後優先使用服務器數據

### 容錯機制
- 如果服務器不可用，仍然使用本地 IndexedDB
- 服務器恢復後會自動同步

## 🔄 API 端點

新增的服務器 API：

- `GET /api/user-data/preferences` - 獲取偏好設定
- `PUT /api/user-data/preferences` - 更新偏好設定
- `GET /api/user-data/messages` - 獲取對話記錄
- `POST /api/user-data/messages` - 添加消息
- `POST /api/user-data/messages/batch` - 批量添加消息
- `DELETE /api/user-data/messages/:id` - 刪除消息
- `DELETE /api/user-data/messages` - 清空所有消息
- `GET /api/user-data/stats` - 獲取統計數據
- `POST /api/user-data/sync` - 手動同步數據

## 🛡️ 數據安全

- 所有數據保存在本地服務器，不會上傳到外部
- JSON 格式存儲，易於備份和遷移
- 自動限制消息數量，防止存儲過大

## 📝 使用說明

### 正常使用
完全透明，無需任何額外操作：
1. 啟動應用
2. 正常使用（設定、聊天等）
3. 所有數據自動保存

### 清除瀏覽器緩存後
1. 重新打開應用
2. 數據自動從服務器恢復
3. 一切如常 ✨

### 手動備份
如需備份，只需複製：
```
server/data/memory/default/
```
文件夾即可。

### 恢復備份
1. 停止服務器
2. 將備份文件放回 `server/data/memory/default/`
3. 重啟服務器

## 🎉 優點

✅ **永久保存** - 清除瀏覽器緩存不會丟失數據
✅ **自動同步** - 無需手動操作
✅ **快速響應** - 本地緩存提供即時反饋
✅ **容錯能力** - 服務器不可用時仍可使用
✅ **易於備份** - 簡單的 JSON 文件格式
✅ **跨設備** - 理論上可在多個瀏覽器間共享數據

## 💡 注意事項

- 首次啟動時如果有大量對話記錄，同步可能需要幾秒鐘
- 建議定期備份 `server/data/memory/` 文件夾
- 多個瀏覽器同時使用可能導致數據衝突（後寫入的會覆蓋）

## 🔧 故障排除

### 數據沒有恢復
1. 檢查服務器是否正常運行
2. 查看瀏覽器控制台是否有錯誤
3. 檢查 `server/data/memory/default/` 是否存在數據文件

### 數據同步失敗
- 服務器控制台會顯示警告
- 數據仍保存在本地，服務器恢復後會重新同步

### 清空所有數據
如需重新開始：
1. 刪除 `server/data/memory/default/` 文件夾內容
2. 清除瀏覽器 IndexedDB（開發者工具 → Application → IndexedDB）
3. 重新啟動應用


